{% set data = block.data or dict.contact %}
<section id="contact" class="section contact">
  <div class="container">
    <div class="section-header">
      <span class="section-label">{{ data.label }}</span>
      <h2 class="section-title">{{ data.title }}</h2>
      <p class="section-desc">{{ data.desc }}</p>
    </div>

    <div class="contact-grid">
      <!-- Info items -->
      <div class="contact-info animate-on-scroll">
        <div class="contact-info-item">
          <span class="contact-info-tag">{{ data.location }}</span>
          <div>
            <h4>{{ data.headquarters }}</h4>
            <p>{{ data.address }}</p>
          </div>
        </div>

        <div class="contact-info-item">
          <span class="contact-info-tag">{{ data.emailLabel }}</span>
          <div>
            <h4>{{ data.generalInquiries }}</h4>
            <p>info@didi.bike</p>
          </div>
        </div>

        <div class="contact-info-item">
          <span class="contact-info-tag">{{ data.businessLabel }}</span>
          <div>
            <h4>{{ data.businessDev }}</h4>
            <p>partners@didi.bike</p>
          </div>
        </div>
      </div>

      <!-- Contact Form -->
      <form noValidate class="glass-card contact-form animate-on-scroll delay-1" id="contactForm" data-locale="{{ locale or 'en' }}">
        <!-- Success Overlay (Initially Hidden) -->
        <div class="contact-success-overlay" id="contactSuccess" style="display: none;">
          <div class="contact-success-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="20 6 9 17 4 12" />
            </svg>
          </div>
          <h3 class="contact-success-title">{{ data.messageSent }}</h3>
          <p class="contact-success-desc">
            {% if locale == 'id' %}
              Terima kasih. Permintaan Anda telah diajukan ke departemen hubungan korporat kami. Perwakilan resmi kami akan segera menghubungi Anda.
            {% else %}
              Thank you. Your request has been filed with our corporate relations department. An official representative will contact you shortly.
            {% endif %}
          </p>
        </div>

        <label for="contact-name" class="sr-only">{{ data.namePlaceholder }}</label>
        <input 
          id="contact-name" 
          name="name" 
          type="text" 
          placeholder="{{ data.namePlaceholder }}" 
          required 
          class="contact-input" 
        />
        
        <label for="contact-email" class="sr-only">{{ data.emailPlaceholder }}</label>
        <input 
          id="contact-email" 
          name="email" 
          type="email" 
          placeholder="{{ data.emailPlaceholder }}" 
          required 
          class="contact-input" 
        />
        
        <label for="contact-type" class="sr-only">{{ data.selectType }}</label>
        <select 
          id="contact-type" 
          name="type" 
          class="contact-input"
          required
        >
          <option value="" disabled selected>{{ data.selectType }}</option>
          <option value="general">{{ data.typeGeneral }}</option>
          <option value="business">{{ data.typeBusiness }}</option>
          <option value="technical">{{ data.typeTechnical }}</option>
          <option value="press">{{ data.typePress }}</option>
        </select>
        
        <label for="contact-message" class="sr-only">{{ data.messagePlaceholder }}</label>
        <textarea 
          id="contact-message" 
          name="message" 
          placeholder="{{ data.messagePlaceholder }}" 
          rows="5" 
          required 
          class="contact-input"
        ></textarea>
        
        <!-- Error Notice -->
        <p class="contact-error-notice" id="contactError" style="display: none;"></p>
        
        <button
          type="submit"
          class="btn btn-primary contact-submit-btn"
          id="contactSubmit"
        >
          {{ data.sendMessage }}
        </button>
      </form>
    </div>
  </div>
</section>
