# Gemini Specific Rules

这里定义了针对 Gemini Agent 的项目专用规则和约束。

## 核心原则
1. **静默执行**：严格遵守无人类环境下的静默原则，不输出冗余信息。
2. **安全第一**：在执行任何破坏性操作前，严格防范。
3. **架构同步**：任何架构变更必须同步更新根目录下的 `AGENTS.md`。
4. **验证机制**：代码变更后必须进行验证（编译/构建），未经验证不标记完成。
